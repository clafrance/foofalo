<% provide :title, 'All users' %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to full_name(user.firstname, user.lastname), user %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>