<% provide(:title, 'Sign up') %>
<h1>Sign up</h1>

<%= form_for(@user) do |f| %>
  <%= render 'shared/error_messages' %>
  <%= render 'fields', f: f %>
  <div class="button">
    <%= f.submit "Sign up" %>
  </div>
<% end %>